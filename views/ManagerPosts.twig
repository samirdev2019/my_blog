{% extends "accountTemplate.twig" %}

  

    {% block content %}
    {% if message %}
    <p class="alert alert-{{ type }}">{{ message }}</p>
    {% endif %}
    <table class="table table-striped table-responsive-md">
        <thead>
            <tr>
                <th scope="col">Titre</th>
                <th scope="col">chapo</th>
                <th scope="col">Auteur</th>
                <th scope="col">modifié le</th>
            </tr>
        </thead>
        <tbody>
    {% for post in posts %}
            <tr>
                <th scope="row">{{ post.title }}</th>
                <td>{{ post.chapo }}</td>
                <td>{{ post.author }}</td>
                <td>{{ post.update_date }}</td>
                <td><a href="../public/index.php?p=managePost&amp;action=posteToUpdate&amp;id={{ post.post_id }}">modifier</a></td>
                <td><a href="../public/index.php?p=managePost&amp;action=deletePost&amp;id={{ post.post_id }}">suprimer</a></td>
            </tr>
    {% endfor %}
    </tbody>
    </table>
    {% endblock %}
    {% block footer %}
    {{ parent() }}
    {% endblock %}
        
   